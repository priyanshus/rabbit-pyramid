{
  "name": "rabbit-pyramid",
  "private": true,
  "version": "0.0.0",
  "description": "A starter project for AngularJS",
  "repository": "https://github.com/angular/angular-seed",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "jasmine-core": "^2.3.4",
    "json-server": "latest",
    "karma": "~0.12",
    "karma-chrome-launcher": "^0.1.12",
    "karma-firefox-launcher": "^0.1.6",
    "karma-jasmine": "^0.3.5",
    "karma-jasmine-jquery": "^0.1.1",
    "karma-junit-reporter": "^0.2.2",
    "karma-spec-reporter": "latest",
    "karma-notify-send-reporter": "latest",
    "protractor": "^2.1.0",
    "shelljs": "^0.2.6",
    "supertest": "^1.0.1",
    "chai": "^1.9.2",
    "chai-as-promised": "^4.1.1",
    "chai-webdriver": "^0.9.4",
    "mocha": "latest"
  },
  "scripts": {
    "startapi": "json-server --watch mocker/db.json&",
    "start": "http-server -a localhost -p 8000 -c-1 & json-server --watch mocker/db.json",

    "test": "karma start karma.conf.js --single-run & npm run integration-test & npm run protractor",
    "karma": "karma start karma.conf.js",
    "integration-test": "mocha integration-tests/*.js",

    "protractor": "protractor e2e-tests/protractor.conf.js"
  }
}
